import 'package:flutter/material.dart';

/// 日历页占位组件（后续可扩展为按日期浏览日记）。
class CalendarPage extends StatelessWidget {
  const CalendarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('日历功能开发中'));
  }
}
